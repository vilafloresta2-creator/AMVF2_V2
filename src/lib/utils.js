export const categories={taxa:"Taxa Mensal",aluguel:"Aluguel Sala",doacao:"Doação",evento:"Evento",manutencao:"Manutenção",conta:"Contas/Despesas",outro:"Outro"};export const slots=["08:00 - 10:00","10:00 - 12:00","13:00 - 15:00","15:00 - 17:00","18:00 - 20:00","20:00 - 22:00"];export const uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now()+"-"+Math.random();export const todayISO=()=>new Date().toISOString().slice(0,10);export const currentMonth=()=>todayISO().slice(0,7);export const brl=v=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(Number(v||0));export const parseMoney=v=>{if(typeof v==="number")return v;let s=String(v??"").trim();if(s.includes(","))s=s.replace(/\./g,"").replace(",",".");return Number(s)||0};export const dateBR=v=>{if(!v)return "";let [y,m,d]=String(v).slice(0,10).split("-");return y&&m&&d?`${d}/${m}/${y}`:v};export const monthLabel=k=>{let [y,m]=String(k).split("-");return `${["Janeiro","Fevereiro","Março","Abril","Maio","Junho","Julho","Agosto","Setembro","Outubro","Novembro","Dezembro"][+m-1]} de ${y}`};export const previousMonth=k=>{let[y,m]=k.split("-").map(Number),d=new Date(y,m-2,1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`};export const nextMonth=k=>{let[y,m]=k.split("-").map(Number),d=new Date(y,m,1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}`};export function csvDownload(filename,rows){let csv=rows.map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(";")).join("\r\n"),b=new Blob(["\ufeff"+csv],{type:"text/csv;charset=utf-8"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download=filename;a.click()}